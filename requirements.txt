# Web app
streamlit>=1.28.1
Pillow>=10.0.1

# Deep learning (CPU build is fine for inference)
torch>=2.0.1
torchvision>=0.15.2

# EfficientNet (primary model)
timm>=0.9.12

# CLIP (zero-shot fallback — downloads ~600 MB on first use)
transformers>=4.33.0

# Training notebook only
scikit-learn>=1.3.0
seaborn>=0.12.2
matplotlib>=3.7.0
numpy>=1.24.3
